# Models directory

Trained models will be saved here:
- audio_stress_model.pkl
- scaler.pkl
- ensemble_voting.pkl (if using separate voting classifier)

These files are generated by running:
```bash
python -m ai_engine.train_ensemble
```